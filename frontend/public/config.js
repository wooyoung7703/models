// Dev-time optional runtime config injected by Vite's static server
// It is safe if this file is missing; index.html references it.
// You can override these at runtime via the browser console if needed.
window.VITE_API_BASE = window.VITE_API_BASE || undefined;
window.VITE_WS_URL = window.VITE_WS_URL || 'ws://127.0.0.1:8022';
